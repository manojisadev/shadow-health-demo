<%= form_with(model: trivia_question, local: true) do |form| %>
  <% if trivia_question.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(trivia_question.errors.count, "error") %> prohibited this trivia_question from being saved:</h2>

      <ul>
      <% trivia_question.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :question %>
    <%= form.text_field :question, id: :trivia_question_question %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
